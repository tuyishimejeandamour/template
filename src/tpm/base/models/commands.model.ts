
export enum BASECOMMANDS {
    NEW = 'new',
    CREARE = 'create',
    HELP = 'help',
    VERSION = '--version',
    INFO = 'info'
}

export enum SECONDARY {
    EXTENSION = 'extension',
    TEMPLATE = 'template'
}

export enum FLAGS {
    T = '-t',
    F = '-f',
    
}